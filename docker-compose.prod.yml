services:
  web:
    build: .
    container_name: personal-site
    ports:
      - "127.0.0.1:5001:5000"  # Only bind to localhost for security
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=sqlite:////app/data/database.db
      - SECRET_KEY=${SECRET_KEY}
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      # Mount a volume for database persistence
      - ./data:/app/data
      # Mount static files for nginx to serve directly (optional)
      - ./static:/app/static:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Nginx container - comment this out if you're using host nginx
  # nginx:
  #   image: nginx:alpine
  #   container_name: personal-site-nginx
  #   ports:
  #     - "8081:80"  # Use port 8081 to avoid conflicts
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./static:/usr/share/nginx/html/static:ro
  #   depends_on:
  #     - web
  #   restart: unless-stopped