---
title: "Hello, World"
description: "Draft placeholder post — visible in dev, hidden in prod."
pubDate: 2026-04-16
draft: true
tags: ["meta"]
---

This is a placeholder post so you can see how blog rendering looks during local
development. It's marked `draft: true` in frontmatter, so it won't ship to
production.

## Headings work

### Code blocks too

```ts
function hello(name: string) {
  return `Hello, ${name}!`;
}
```

## Lists

- Markdown is supported
- So is MDX (you can drop React/Astro components in here later)
- Syntax highlighting comes from shiki

> Blockquotes look like this.

Delete this file or flip `draft: false` when you're ready to publish something
real.
